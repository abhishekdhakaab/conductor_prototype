## Dowload the following dependencies : 
1. npm
2. postgre SQL

## Run this  command : 
git clone https://github.com/<your-team>/conductor.git && cd conductor && npm install && psql -f sql/snapshot/conductor_snapshot_latest.sql && npm start
